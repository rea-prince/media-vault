package mediavault.controllers;

import mediavault.models.MediaEntry;
import mediavault.models.MediaVault;

import java.util.ArrayList;

import javafx.scene.control.Button;

import javafx.event.ActionEvent;

import javafx.fxml.FXML;

import javafx.scene.control.ChoiceBox;
import javafx.scene.control.TextField;

public class SortController 
{
    public void ascendTitleSort (ArrayList<MediaEntry> entries)
    {
        for (int i = 0; i < entries.size(); i++)
        {

        }
    }

    public void descendTitleSort (ArrayList<MediaEntry> entries)
    {
        for (int i = 0; i < entries.size(); i++)
        {
            
        }
    }

    public void ascendYearSort (ArrayList<MediaEntry> entries)
    {
        for (int i = 0; i < entries.size(); i++)
        {

        }
    }

    public void descendYearSort (ArrayList<MediaEntry> entries)
    {
        for (int i = 0; i < entries.size(); i++)
        {
            
        }
    }
}
